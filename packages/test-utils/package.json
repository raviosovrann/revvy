{
  "name": "@revvy/test-utils",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src/ --fix",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@revvy/config": "workspace:*",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.5.4"
  }
}
